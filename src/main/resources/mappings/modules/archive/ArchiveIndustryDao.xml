<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.archive.dao.ArchiveIndustryDao">
    
	<sql id="archiveIndustryColumns">
		a.id AS "id",
		a.industry_name AS "industryName",
		a.industry_code AS "industryCode",
		a.ext_field1 AS "extField1",
		a.ext_field2 AS "extField2",
		a.ext_field3 AS "extField3",
		a.remark AS "remark"
	</sql>
	
	<sql id="archiveIndustryJoins">
	</sql>
    
	<select id="get" resultType="ArchiveIndustry">
		SELECT 
			<include refid="archiveIndustryColumns"/>
		FROM archive_industry a
		<include refid="archiveIndustryJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="ArchiveIndustry">
		SELECT 
			<include refid="archiveIndustryColumns"/>
		FROM archive_industry a
		<include refid="archiveIndustryJoins"/>
		<where>
			<if test="industryCode != null and industryCode != ''">
				AND a.industry_code LIKE
				<if test="dbName == 'oracle'">'%'||#{industryCode}||'%'</if>
				<if test="dbName == 'mssql'">'%'+#{industryCode}+'%'</if>
				<if test="dbName == 'mysql'">CONCAT('%', #{industryCode}, '%')</if>
			</if>
			<if test="industryName != null and industryName != ''">
				AND a.industry_name LIKE
				<if test="dbName == 'oracle'">'%'||#{industryName}||'%'</if>
				<if test="dbName == 'mssql'">'%'+#{industryName}+'%'</if>
				<if test="dbName == 'mysql'">CONCAT('%', #{industryName}, '%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="ArchiveIndustry">
		SELECT 
			<include refid="archiveIndustryColumns"/>
		FROM archive_industry a
		<include refid="archiveIndustryJoins"/>
		<where>
			<if test="industryCode != null and industryCode != ''">
				AND a.industry_code LIKE
				<if test="dbName == 'oracle'">'%'||#{industryCode}||'%'</if>
				<if test="dbName == 'mssql'">'%'+#{industryCode}+'%'</if>
				<if test="dbName == 'mysql'">CONCAT('%', #{industryCode}, '%')</if>
			</if>
			<if test="industryName != null and industryName != ''">
				AND a.industry_name LIKE
				<if test="dbName == 'oracle'">'%'||#{industryName}||'%'</if>
				<if test="dbName == 'mssql'">'%'+#{industryName}+'%'</if>
				<if test="dbName == 'mysql'">CONCAT('%', #{industryName}, '%')</if>
			</if>
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO archive_industry(
			industry_name,
			industry_code,
			ext_field1,
			ext_field2,
			ext_field3,
			remark
		) VALUES (
			#{industryName},
			#{industryCode},
			#{extField1},
			#{extField2},
			#{extField3},
			#{remark}
		)
	</insert>
	
	<update id="update">
		UPDATE archive_industry SET 	
			industry_name = #{industryName},
			industry_code = #{industryCode},
			ext_field1 = #{extField1},
			ext_field2 = #{extField2},
			ext_field3 = #{extField3},
			remark = #{remark}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM archive_industry
		WHERE id = #{id}
	</update>
	
</mapper>