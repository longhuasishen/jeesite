<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.archive.dao.ArchiveRepertoryDao">
    
	<sql id="archiveRepertoryColumns">
		a.id AS "id",
		a.repertory_name AS "repertoryName",
		a.repertory_code AS "repertoryCode"
	</sql>
	
	<sql id="archiveRepertoryJoins">
	</sql>
    
	<select id="get" resultType="ArchiveRepertory">
		SELECT 
			<include refid="archiveRepertoryColumns"/>
		FROM archive_repertory a
		<include refid="archiveRepertoryJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="ArchiveRepertory">
		SELECT 
			<include refid="archiveRepertoryColumns"/>
		FROM archive_repertory a
		<include refid="archiveRepertoryJoins"/>
		<where>
			<if test="repertoryCode != null and repertoryCode != ''">
				AND a.repertory_code LIKE
				<if test="dbName == 'oracle'">'%'||#{repertoryCode}||'%'</if>
				<if test="dbName == 'mssql'">'%'+#{repertoryCode}+'%'</if>
				<if test="dbName == 'mysql'">CONCAT('%', #{repertoryCode}, '%')</if>
			</if>
			<if test="repertoryName != null and repertoryName != ''">
				AND a.repertory_name LIKE
				<if test="dbName == 'oracle'">'%'||#{repertoryName}||'%'</if>
				<if test="dbName == 'mssql'">'%'+#{repertoryName}+'%'</if>
				<if test="dbName == 'mysql'">CONCAT('%', #{repertoryName}, '%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="ArchiveRepertory">
		SELECT 
			<include refid="archiveRepertoryColumns"/>
		FROM archive_repertory a
		<include refid="archiveRepertoryJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO archive_repertory(
			repertory_name,
			repertory_code
		) VALUES (
			#{repertoryName},
			#{repertoryCode}
		)
	</insert>
	
	<update id="update">
		UPDATE archive_repertory SET 	
			repertory_name = #{repertoryName},
			repertory_code = #{repertoryCode}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM archive_repertory
		WHERE id = #{id}
	</update>
	
</mapper>