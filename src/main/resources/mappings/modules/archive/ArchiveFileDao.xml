<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.archive.dao.ArchiveFileDao">
    
	<sql id="archiveFileColumns">
		a.id AS "id",
		a.file_name AS "fileName",
		a.file_code AS "fileCode",
		a.ext_field1 AS "extField1",
		a.ext_field2 AS "extField2",
		a.ext_field3 AS "extField3",
		a.remark AS "remark"
	</sql>
	
	<sql id="archiveFileJoins">
	</sql>
    
	<select id="get" resultType="ArchiveFile">
		SELECT 
			<include refid="archiveFileColumns"/>
		FROM archive_file a
		<include refid="archiveFileJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="ArchiveFile">
		SELECT 
			<include refid="archiveFileColumns"/>
		FROM archive_file a
		<include refid="archiveFileJoins"/>
		<where>
			<if test="fileCode != null and fileCode != ''">
				AND a.file_code LIKE
				<if test="dbName == 'oracle'">'%'||#{fileCode}||'%'</if>
				<if test="dbName == 'mssql'">'%'+#{fileCode}+'%'</if>
				<if test="dbName == 'mysql'">CONCAT('%', #{fileCode}, '%')</if>
			</if>
			<if test="fileName != null and fileName != ''">
				AND a.file_name LIKE
				<if test="dbName == 'oracle'">'%'||#{fileName}||'%'</if>
				<if test="dbName == 'mssql'">'%'+#{fileName}+'%'</if>
				<if test="dbName == 'mysql'">CONCAT('%', #{fileName}, '%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="ArchiveFile">
		SELECT 
			<include refid="archiveFileColumns"/>
		FROM archive_file a
		<include refid="archiveFileJoins"/>
		<where>
			<if test="fileCode != null and fileCode != ''">
				AND a.file_code LIKE
				<if test="dbName == 'oracle'">'%'||#{fileCode}||'%'</if>
				<if test="dbName == 'mssql'">'%'+#{fileCode}+'%'</if>
				<if test="dbName == 'mysql'">CONCAT('%', #{fileCode}, '%')</if>
			</if>
			<if test="fileName != null and fileName != ''">
				AND a.file_name LIKE
				<if test="dbName == 'oracle'">'%'||#{fileName}||'%'</if>
				<if test="dbName == 'mssql'">'%'+#{fileName}+'%'</if>
				<if test="dbName == 'mysql'">CONCAT('%', #{fileName}, '%')</if>
			</if>
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO archive_file(
			file_name,
			file_code,
			ext_field1,
			ext_field2,
			ext_field3,
			remark
		) VALUES (
			#{fileName},
			#{fileCode},
			#{extField1},
			#{extField2},
			#{extField3},
			#{remark}
		)
	</insert>
	
	<update id="update">
		UPDATE archive_file SET 	
			file_name = #{fileName},
			file_code = #{fileCode},
			ext_field1 = #{extField1},
			ext_field2 = #{extField2},
			ext_field3 = #{extField3},
			remark = #{remark}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM archive_file
		WHERE id = #{id}
	</update>
	
</mapper>