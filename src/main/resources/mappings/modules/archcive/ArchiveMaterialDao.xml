<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.archcive.dao.ArchiveMaterialDao">
    
	<sql id="archiveMaterialColumns">
		a.id AS "id",
		a.material_name AS "materialName"
	</sql>
	
	<sql id="archiveMaterialJoins">
	</sql>
    
	<select id="get" resultType="ArchiveMaterial">
		SELECT 
			<include refid="archiveMaterialColumns"/>
		FROM archive_material a
		<include refid="archiveMaterialJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="ArchiveMaterial">
		SELECT 
			<include refid="archiveMaterialColumns"/>
		FROM archive_material a
		<include refid="archiveMaterialJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="ArchiveMaterial">
		SELECT 
			<include refid="archiveMaterialColumns"/>
		FROM archive_material a
		<include refid="archiveMaterialJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO archive_material(
			id,
			material_name
		) VALUES (
			#{id},
			#{materialName}
		)
	</insert>
	
	<update id="update">
		UPDATE archive_material SET 	
			material_name = #{materialName}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM archive_material
		WHERE id = #{id}
	</update>
	
</mapper>